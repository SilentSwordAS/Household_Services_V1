# Household Services V1
### Steps to Run the App

#### 1. Initialize and activate your python virtual environment following these instructions, (https://docs.python.org/3/library/venv.html)
#### 2. Run this command in your virtual environment in the terminal => pip install -r requirements.txt
#### 3. Run python main.py
